{
  "name": "static-github-pages-site",
  "version": "1.0.0",
  "description": "A simple static website for GitHub Pages with navigation and styling.",
  "main": "index.html",
  "scripts": {
    "start": "live-server",  // To run a local dev server
    "build": "postcss styles.css -o dist/styles.min.css && html-minifier-terser -o dist/index.html --collapse-whitespace index.html" // To minify assets
  },
  "author": "Your Name",
  "license": "MIT",
  "devDependencies": {
    "live-server": "^1.2.2",
    "postcss-cli": "^10.1.0",
    "autoprefixer": "^10.4.14",
    "html-minifier-terser": "^7.1.0"
  }
}
